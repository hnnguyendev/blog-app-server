apply plugin: "war"

bootWar {
    mainClass = "dev.hnnguyen.blog.BlogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
